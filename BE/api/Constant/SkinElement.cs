namespace api.Constant
{
    public enum SkinElement
    {
        Moisture = 1,    // Đánh giá độ ẩm (Dry/Oily)
        Sensitivity = 2, // Đánh giá độ nhạy cảm (Sensitive/Resistant)
        Pigmentation = 3,// Đánh giá sắc tố (Pigmented/Non-pigmented)
        Elasticity = 4   // Đánh giá độ đàn hồi (Tight/Wrinkled)
    }
}